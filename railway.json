{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install --no-dev --no-interaction --optimize-autoloader --no-scripts && composer dump-autoload --optimize --no-dev && npm ci && npm run build"
  },
  "deploy": {
    "startCommand": "php artisan route:cache && php artisan view:cache",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

